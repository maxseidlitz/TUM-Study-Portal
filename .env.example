# Optional: Gemini API Key for the Electron main process (alternative to storing in Settings).
# Copy to `.env` and load before `npm run dev` if your toolchain injects env vars — never commit secrets.
GEMINI_API_KEY=
